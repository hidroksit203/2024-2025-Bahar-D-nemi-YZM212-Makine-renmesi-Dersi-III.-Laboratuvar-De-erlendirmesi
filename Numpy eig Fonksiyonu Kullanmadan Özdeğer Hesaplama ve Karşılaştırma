Numpy eig Fonksiyonu Kullanmadan Özdeğer Hesaplama ve Karşılaştırma
Amaç
Bu soruda, hazır numpy.linalg.eig fonksiyonunu kullanmadan özdeğerlerin hesaplandığı bir çalışmayı tekrar edip, ardından aynı matriste NumPy’ın eig fonksiyonu ile sonuçları karşılaştırmamız isteniyor.

Referans Proje
📌 Kaynak:
https://github.com/LucasBN/Eigenvalues-and-Eigenvectors
(Erişim: 7 Nisan 2025)

Bu çalışmada özdeğerler, karakteristik polinom kullanılarak manuel olarak hesaplanmaktadır. Özdeğerlerin bulunduğu temel yöntem aşağıdaki gibidir:

det(A−λI)=0
Bu denklem çözüldüğünde, 

λ (özdeğerler) elde edilir. Daha sonra bu değerler ile ilgili özvektörler bulunur.

Örnek Matris ile Uygulama
1. Elle Hesaplama (Karakteristik Denklem ile)
import numpy as np

# Örnek kare matris
A = np.array([[4, -2],
              [1,  1]])

# Karakteristik polinomun determinantını manuel hesaplama
# |A - λI| = 0 → (4-λ)(1-λ) + 2 = 0
# Detaylı şekilde yapılabilir ama NumPy'dan yardım alarak karakteristik polinomu çıkartabiliriz
coeff = np.poly(A)  # Karakteristik polinomun katsayıları
eigenvalues_manual = np.roots(coeff)

print("Elle hesaplanan özdeğerler:", eigenvalues_manual)


NumPy ile eig Kullanarak Hesaplama
eigenvalues_numpy, eigenvectors_numpy = np.linalg.eig(A)

print("NumPy ile hesaplanan özdeğerler:", eigenvalues_numpy)
print("NumPy ile hesaplanan özvektörler:\n", eigenvectors_numpy)

Karşılaştırma
Özdeğerler her iki yöntemde de aynı çıkacaktır (sıra farklı olabilir ama matematiksel olarak aynıdır). Özvektörler de doğrultu olarak aynı olacak, fakat büyüklükleri veya işaretleri farklı olabilir çünkü özvektörler sabit çarpanla değişebilir.


Sonuç ve Yorum
Bu karşılaştırma gösteriyor ki, özdeğer hesaplama teorik olarak karakteristik denklem üzerinden yapılabilir, ancak pratikte büyük matrisler ve karmaşık veriler için bu yaklaşım hem zaman alıcı hem de hataya açıktır. Bu yüzden NumPy gibi kütüphaneler sayesinde hem doğruluk hem de hız açısından avantaj sağlanır.

Not: Özvektörlerin normları farklı olabilir; bu hesaplama yöntemi farkından kaynaklanır ve matematiksel olarak geçerlidir.
